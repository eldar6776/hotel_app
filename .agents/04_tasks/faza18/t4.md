# ZADATAK T18.4: GDPR compliance
**Status:** [ ] PENDING
**Faza:** 18 - Audit, optimizacija i deployment
**Modul:** backend/src/HotelPro.Api/Services/GDPR/
**Vlasnik:** AI Coding Agent

## 1. CILJ
Implementirati GDPR compliance mehanizme ukljucujuci guest data export, data forget (right to erasure), consent management, privacy log i data retention politike.

## 2. KONTEKST (Referenca)
- FSD 07: Gosti i CRM, sekcija 6 — GDPR compliance

## 3. TEHNICKI ZAHTJEVI

### 3.1 Data Export (Right to Access)
Lokacija: `Services/GDPR/GdprService.cs`
```
GET /api/gdpr/export/{guestId}
    — generise ZIP fajl sa svim podacima gosta:
      - Licni podaci (Guest tabela)
      - Rezervacije (Booking)
      - Transakcije (PaymentTransaction, FolioEntry)
      - Komunikacija (Email log, SMS log)
      - Audit log (samo ono sto se tice guesta)
```
- ZIP fajl sadrzi JSON fajlove (machine-readable)
- Generisanje moze biti async — za velike exporte koristiti background job
- Export se cuva 7 dana, zatim brise

### 3.2 Data Forget (Right to Erasure)
```
POST /api/gdpr/forget/{guestId}
    — anonimizuje sve podatke gosta:
      - Ime, prezime -> "[REDACTED]"
      - Email -> "redacted-{id}@anonymous.hotel"
      - Telefon -> null
      - Adresa -> null
      - Dokumenti -> obrisani
```
- Anonimizacija je irreversible
- Booking i transakcije ostaju (ne smiju se brisati zbog finansijske evidencije)
- Zadrzava se samo: GuestId, Booking reference, iznosi, datumi
- Kreira se `GdprForgetRecord` sa timestampom i admin ID-jem

### 3.3 Consent Management
Entitet: `GuestConsent.cs`
```
GuestConsent.cs
  - Id (Guid), GuestId (Guid)
  - ConsentType: enum (Marketing, DataProcessing, ThirdPartySharing, Cookies)
  - IsGranted (bool)
  - GrantedAt, RevokedAt (DateTime?)
  - IpAddress (string)
  - ConsentVersion (string) — verzija politike
```
- API za guest consent management
- Frontend: consent checkbox na check-in formi
- Log: svaka promjena consenta se biljezi u audit log

### 3.4 Data Retention Policies
- Automatsko brisanje/anomimizacija po isteku retention perioda:
  - Guest dokumenti: 30 dana poslije check-outa
  - Chat poruke: 12 mjeseci
  - Audit log: 5 godina (zakonski)
  - Email log: 3 godine
  - Guest racuni: 10 godina (zakonski)
- Background job: `DataRetentionService.cs` — pokrece se jednom dnevno

### 3.5 Privacy Log
Entitet: `PrivacyLog.cs`
```
PrivacyLog.cs
  - Id (Guid), GuestId (Guid)
  - ActionType: enum (DataExport, DataForget, ConsentChange, DataAccess)
  - PerformedBy (string) — "Guest", "Admin:{id}", "System"
  - IpAddress (string)
  - Details: JSON kolona
  - CreatedAt (DateTime)
```
- Svi GDPR-related dogadjaji se biljeze
- Log je read-only (append-only)
- Dostupan adminima za audit

## 4. DEFINICIJA GOTOVOG (Definition of Done)
- [ ] Data export endpoint sa ZIP generisanjem
- [ ] Data forget endpoint sa anonimizacijom
- [ ] `GuestConsent` entitet i consent management API
- [ ] `DataRetentionService` background job
- [ ] `PrivacyLog` entitet sa audit trailom
- [ ] Frontend: consent checkbox na check-in formi
- [ ] Svi endpointi zašticeni (Admin rola za export/forget)
- [ ] `dotnet build` prolazi bez gresaka
- [ ] Azuriran `STATUS.md`

## 5. RESTRIKCIJE
- Anonimizacija mora biti irreversible — ne koristiti enkripciju (moze se dekriptovati)
- Finansijski podaci se nikada ne brisu — samo se anonimiziraju licni podaci
- Privacy log se nikada ne brise (append-only)
- Svi GDPR endpointi zahtijevaju audit trail entry
- Export fajlovi moraju biti u JSON formatu (machine-readable)
