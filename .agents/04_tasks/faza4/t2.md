# ZADATAK T4.2: Design System — boje, tipografija, spacing, Dark Mode
**Status:** [x] COMPLETED
**Faza:** 4 - Frontend Foundation
**Modul:** frontend/
**Vlasnik:** AI Coding Agent

## 1. CILJ
Kreirati Design System sa HSL paletom boja (Primary, Accent, Success, Warning, Error), Inter fontom, custom Tailwind bojama i dark mode podrskom (class strategy).

## 2. KONTEKST (Referenca)
- FSD 3: Frontend Foundation, sekcija 3 — Design System

## 3. TEHNICKI ZAHTJEVI

### 3.1 Tailwind konfiguracija
Lokacija: `frontend/tailwind.config.ts`

```typescript
import type { Config } from 'tailwindcss'

const config: Config = {
  darkMode: 'class',
  content: ['./src/**/*.{ts,tsx,mdx}'],
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },
      colors: {
        primary: {
          50: 'hsl(var(--color-primary-50) / <alpha-value>)',
          100: 'hsl(var(--color-primary-100) / <alpha-value>)',
          200: 'hsl(var(--color-primary-200) / <alpha-value>)',
          300: 'hsl(var(--color-primary-300) / <alpha-value>)',
          400: 'hsl(var(--color-primary-400) / <alpha-value>)',
          500: 'hsl(var(--color-primary-500) / <alpha-value>)',
          600: 'hsl(var(--color-primary-600) / <alpha-value>)',
          700: 'hsl(var(--color-primary-700) / <alpha-value>)',
          800: 'hsl(var(--color-primary-800) / <alpha-value>)',
          900: 'hsl(var(--color-primary-900) / <alpha-value>)',
        },
        accent: {
          50: 'hsl(var(--color-accent-50) / <alpha-value>)',
          100: 'hsl(var(--color-accent-100) / <alpha-value>)',
          // ... 200-900
        },
        success: {
          DEFAULT: 'hsl(var(--color-success))',
          light: 'hsl(var(--color-success-light))',
          dark: 'hsl(var(--color-success-dark))',
        },
        warning: {
          DEFAULT: 'hsl(var(--color-warning))',
          light: 'hsl(var(--color-warning-light))',
          dark: 'hsl(var(--color-warning-dark))',
        },
        error: {
          DEFAULT: 'hsl(var(--color-error))',
          light: 'hsl(var(--color-error-light))',
          dark: 'hsl(var(--color-error-dark))',
        },
      },
      spacing: {
        '4.5': '1.125rem',
        '18': '4.5rem',
        '88': '22rem',
        '100': '25rem',
        '120': '30rem',
      },
      borderRadius: {
        '4xl': '2rem',
      },
    },
  },
  plugins: [],
}
export default config
```

### 3.2 HSL varijable (Light theme)
Lokacija: `frontend/src/styles/globals.css`

```css
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    /* Primary: Blue */
    --color-primary-50: 213 92% 95%;
    --color-primary-100: 214 91% 87%;
    --color-primary-200: 214 89% 74%;
    --color-primary-300: 214 87% 58%;
    --color-primary-400: 214 89% 48%;
    --color-primary-500: 214 90% 42%;
    --color-primary-600: 214 88% 36%;
    --color-primary-700: 215 85% 30%;
    --color-primary-800: 215 82% 24%;
    --color-primary-900: 216 78% 18%;

    /* Accent: Gold */
    --color-accent-50: 42 100% 95%;
    --color-accent-100: 42 96% 84%;
    --color-accent-200: 42 93% 70%;
    --color-accent-300: 42 90% 55%;
    --color-accent-400: 42 92% 45%;
    --color-accent-500: 42 90% 38%;
    --color-accent-600: 42 85% 32%;
    --color-accent-700: 42 80% 26%;
    --color-accent-800: 42 75% 20%;
    --color-accent-900: 42 70% 14%;

    /* Status colors */
    --color-success: 142 71% 45%;
    --color-success-light: 142 71% 55%;
    --color-success-dark: 142 71% 35%;

    --color-warning: 38 92% 50%;
    --color-warning-light: 38 92% 60%;
    --color-warning-dark: 38 92% 40%;

    --color-error: 0 84% 60%;
    --color-error-light: 0 84% 70%;
    --color-error-dark: 0 84% 50%;

    /* Neutral / Surface */
    --color-surface: 0 0% 100%;
    --color-surface-secondary: 220 14% 96%;
    --color-surface-tertiary: 220 13% 91%;
    --color-border: 220 13% 85%;
    --color-text: 220 15% 10%;
    --color-text-secondary: 220 10% 45%;
  }

  .dark {
    --color-surface: 220 18% 10%;
    --color-surface-secondary: 220 16% 15%;
    --color-surface-tertiary: 220 14% 20%;
    --color-border: 220 13% 25%;
    --color-text: 220 10% 90%;
    --color-text-secondary: 220 10% 60%;
  }
}
```

### 3.3 Inter font
- Google Fonts: `Inter` (300, 400, 500, 600, 700)
- Fallback: `system-ui, sans-serif`
- Uvezen u `globals.css` preko `@import url(...)`

### 3.4 Dark Mode strategy
- `class` strategy (ne `media`)
- Toggle preko `document.documentElement.classList.toggle('dark')`
- Provider: `ThemeProvider` komponenta koja cuva preferencu u `localStorage`
- Inicijalno: prati `prefers-color-scheme` media query

### 3.5 ThemeProvider komponenta
Lokacija: `frontend/src/components/providers/ThemeProvider.tsx`

```typescript
'use client'

import { createContext, useContext, useEffect, useState } from 'react'

type Theme = 'light' | 'dark'

const ThemeContext = createContext<{
  theme: Theme
  toggleTheme: () => void
}>({ theme: 'light', toggleTheme: () => {} })

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setTheme] = useState<Theme>('light')

  useEffect(() => {
    const stored = localStorage.getItem('theme') as Theme | null
    if (stored) {
      setTheme(stored)
      document.documentElement.classList.toggle('dark', stored === 'dark')
    } else {
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches
      setTheme(prefersDark ? 'dark' : 'light')
      document.documentElement.classList.toggle('dark', prefersDark)
    }
  }, [])

  const toggleTheme = () => {
    const next = theme === 'light' ? 'dark' : 'light'
    setTheme(next)
    localStorage.setItem('theme', next)
    document.documentElement.classList.toggle('dark', next === 'dark')
  }

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  )
}

export const useTheme = () => useContext(ThemeContext)
```

### 3.6 Spacing sistem
- Baziran na Tailwind standardnom spacing-u (4px = 1 unit)
- Dodatne vrijednosti: 4.5 (18px), 18 (72px), 88 (352px), 100 (400px), 120 (480px)
- Gap: `gap-2` (8px), `gap-3` (12px), `gap-4` (16px), `gap-6` (24px)

### 3.7 Tipografija
- `h1`: 2.25rem/36px, semibold
- `h2`: 1.875rem/30px, semibold
- `h3`: 1.5rem/24px, semibold
- `h4`: 1.25rem/20px, medium
- `body`: 0.938rem/15px, regular
- `small`: 0.813rem/13px, regular
- `caption`: 0.75rem/12px, regular

## 4. DEFINICIJA GOTOVOG (Definition of Done)
- [ ] `tailwind.config.ts` konfigurisan sa HSL bojama, Inter fontom, spacing-om
- [ ] `globals.css` sadrzi HSL varijable za light i dark theme
- [ ] Dark mode koristi `class` strategiju
- [ ] `ThemeProvider` komponenta sa localStorage perzistencijom
- [ ] Inter font uvezen i ispravno se prikazuje
- [ ] Status boje (success, warning, error) definisane
- [ ] Azuriran `STATUS.md`

## 5. RESTRIKCIJE
- Sve boje moraju biti HSL (ne HEX, ne RGB)
- Dark mode se ne smije flicker-ovati pri ucitavanju (provjeriti localStorage prije render-a)
- Ne koristiti CSS-in-JS biblioteke — samo Tailwind CSS
- Inter font se ucitava sa Google Fonts (fallback: system-ui)
- Primarna boja je plava (hue ~214), accent je zlatna (hue ~42)
