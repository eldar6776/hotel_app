# ZADATAK T12.4: Integracija sa telefonskom centralom (PABX/CDR)
**Status:** [ ] PENDING
**Faza:** 12 - Integracije sa lokalnim hardverom
**Modul:** backend/src/HotelPro.Bridge/Devices/Pabx
**Vlasnik:** AI Coding Agent

## 1. CILJ
Implementirati integraciju sa hotelskom telefonskom centralom (PABX) za import CDR (Call Detail Record) podataka, mapiranje telefonskih ekstenzija na sobe, tarifiranje poziva i automatsko knjiženje na gostov folio.

## 2. KONTEKST (Referenca)
- FSD_12: Telefonija, sekcija 2-4, 7
- Zavisnost: T12.1 (bridge servis)

## 3. TEHNICKI ZAHTJEVI

### 3.1 CDR import
- `PabxCdrReader` klasa za čitanje CDR fajlova sa PABX-a (CSV/JSON format)
- Podržani formati: Alcatel, Siemens, Panasonic (konfigurabilno)
- `GET /api/bridge/pabx/cdr?from=...&to=...` — preuzimanje CDR zapisa
- Automatski import: background job na bridge-u svakih 5 minuta
- Ručni import: admin može upload-ovati CDR fajl

### 3.2 Mapiranje ekstenzija → sobe
- Tabela `phone_extensions`:
  - `extension` (VARCHAR, PK) — npr. "201"
  - `room_id` (UUID, FK → rooms.id, nullable)
  - `description` (VARCHAR, nullable)
- API: `GET /api/phone-extensions`, `POST /api/phone-extensions`, `PUT /api/phone-extensions/{extension}`
- Prilikom check-ina, sobarica može dodijeliti ekstenziju sobi

### 3.3 Tarifiranje
- Tabela `call_tariffs`:
  - `destination_prefix` (VARCHAR) — npr. "00" za medjunarodne, "06" za mobilne
  - `rate_per_minute` (DECIMAL)
  - `connection_fee` (DECIMAL)
  - `description` (VARCHAR)
- Tarifa se računa: `connection_fee + (duration_minutes * rate_per_minute)`
- Neidentifikovani pozivi (bez mapirane sobe) se tarifiraju ali ne knjiže na folio

### 3.4 Auto-posting na folio
- Automatsko knjiženje tarifiranih poziva na gostov folio (T8.5)
- Stavka na foliju: `"Telephone - {called_number} ({duration})"` sa iznosom
- Pozivi napravljeni prije check-ina ili poslije check-outa se ne knjiže

### 3.5 Wake-up call
- `POST /api/housekeeping/wake-up-call` — zakazivanje
- Bridge prosljeđuje PABX-u komandu za wake-up call
- `roomNumber`, `time`, `language`
- Lista aktivnih wake-up callova: `GET /api/housekeeping/wake-up-call`

## 4. DEFINICIJA GOTOVOG (Definition of Done)
- [ ] CDR import radi (automatski i ručni)
- [ ] Mapiranje ekstenzija → soba je implementirano
- [ ] Tarifiranje radi po definisanim tarifama
- [ ] Auto-posting na folio funkcioniše
- [ ] Wake-up call API je implementiran
- [ ] Unit testovi za tarifiranje i knjiženje
- [ ] Azuriran `STATUS.md`

## 5. RESTRIKCIJE
- CDR fajlovi se ne brišu nakon importa — samo se markiraju kao `IMPORTED`
- Tarifiranje se vrši po najdužem prefiksu (najspecifičnija tarifa ima prednost)
- Wake-up call mora raditi čak i ako gost nema mapiranu ekstenziju (putem sobe)
- Pozivi sa trajanjem 0 sekundi se ne tarifiraju (neuspjeli pozivi)
