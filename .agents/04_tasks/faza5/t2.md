# ZADATAK T5.2: Frontend — interaktivni pregled soba (Grid/Floor Plan)
**Status:** [ ] PENDING
**Faza:** 5 - Sobe i Upravljanje Sobama
**Modul:** frontend/
**Vlasnik:** AI Coding Agent

## 1. CILJ
Kreirati interaktivni pregled soba sa Grid prikazom i Floor Plan pogledom, color-coded statusima i klikom za detalje.

## 2. KONTEKST (Referenca)
- FSD_04: Sobe i Upravljanje Sobama
- Zavisi od: T5.1 (API), T4.3 (Layout)

## 3. TEHNICKI ZAHTJEVI

### 3.1 RoomGrid stranica
Lokacija: `frontend/src/app/(app)/rooms/page.tsx`

- Route: `/rooms`
- Dva view moda: Grid (default) i Floor Plan (toggle)
- Filter bar na vrhu: status filter, building filter, floor filter, search
- Refresh dugme

### 3.2 Grid view
- 4-6 kartica po redu (responsive grid)
- Svaka kartica:
  - Broj sobe (veliki, bold)
  - Status badge (boja)

### 3.3 RoomCard komponenta
Lokacija: `frontend/src/components/rooms/RoomCard.tsx`

```typescript
interface RoomCardProps {
  room: RoomDto
  onClick: (room: RoomDto) => void
}

const statusColors = {
  Free: 'bg-success text-white',
  Occupied: 'bg-primary-500 text-white',
  Reserved: 'bg-warning text-white',
  Dirty: 'bg-orange-500 text-white',
  OutOfOrder: 'bg-error text-white',
  OutOfService: 'bg-gray-500 text-white',
}

const statusLabels = {
  Free: 'Slobodna',
  Occupied: 'Zauzeta',
  Reserved: 'Rezervirana',
  Dirty: 'Prijava',
  OutOfOrder: 'Van funkcije',
  OutOfService: 'Van servisa',
}

export function RoomCard({ room, onClick }: RoomCardProps) {
  return (
    <div
      onClick={() => onClick(room)}
      className="group cursor-pointer rounded-xl border border-border bg-surface p-4 shadow-sm transition-all hover:shadow-md hover:border-primary-300"
    >
      <div className="mb-3 flex items-center justify-between">
        <span className="text-lg font-bold text-text">{room.roomNumber}</span>
        <span className={`rounded-full px-2.5 py-0.5 text-xs font-medium ${statusColors[room.status]}`}>
          {statusLabels[room.status]}
        </span>
      </div>
      <div className="space-y-1 text-sm text-text-secondary">
        <p>{room.roomTypeName}</p>
        <p>Kapacitet: {room.baseCapacity}-{room.maxCapacity} osobe</p>
        <p>Kat: {room.floor} | {room.buildingName}</p>
      </div>
    </div>
  )
}
```

### 3.4 Floor Plan view
- Prikaz po spratovima (tabovi ili accordion)
- SVG/div-based layout soba na spratu
- Pozicioniranje soba prema koordinatama (ili automatski grid)
- Status boje identicne Grid view-u
- Mouse hover: highlight sobu

### 3.5 RoomDetail modal
- Klik na karticu otvara modal sa detaljima sobe
- Tabovi: Informacije, Trenutni gost (ako je zauzeta), Istorija, Housekeeping

```typescript
interface RoomDetailProps {
  room: RoomDto
  isOpen: boolean
  onClose: () => void
}
```

Sadrzaj modala:
- Osnovne informacije (broj, kat, zgrada, tip, kapacitet, cijena)
- Status sa mogucnoscu promjene (ako korisnik ima permisiju)
- Trenutni gost (ako je status Occupied): ime, datum check-in, expected check-out
- Lista zadnjih 5 housekeeping logova
- Quick actions: "Check-in", "Check-out", "Oznaci cistom", "Prijavi kvar"

### 3.6 RoomService (frontend)
Lokacija: `frontend/src/lib/rooms/room-service.ts`

```typescript
export const roomService = {
  async getRooms(filters?: RoomFilters): Promise<RoomDto[]> {
    const params = new URLSearchParams()
    if (filters?.status?.length) params.append('status', filters.status.join(','))
    if (filters?.buildingId) params.append('buildingId', filters.buildingId)
    if (filters?.floor) params.append('floor', String(filters.floor))
    if (filters?.search) params.append('search', filters.search)
    const response = await apiClient.get(`/rooms?${params}`)
    return response.data
  },
  async getRoom(id: string): Promise<RoomDto> {
    const response = await apiClient.get(`/rooms/${id}`)
    return response.data
  },
  async updateStatus(id: string, status: RoomStatus): Promise<void> {
    await apiClient.patch(`/rooms/${id}/status`, status)
  },
}
```

### 3.7 Status color-coded legenda
Iznad grid-a:
```typescript
const legend = [
  { color: 'bg-success', label: 'Slobodna' },
  { color: 'bg-primary-500', label: 'Zauzeta' },
  { color: 'bg-warning', label: 'Rezervirana' },
  { color: 'bg-orange-500', label: 'Prijava' },
  { color: 'bg-error', label: 'Van funkcije' },
  { color: 'bg-gray-500', label: 'Van servisa' },
]
```

### 3.8 Filter bar
- Dropdown za status (multi-select)
- Dropdown za zgradu (ucitan sa /buildings)
- Dropdown za sprat (1-5)
- Search input (pretraga po broju sobe)
- "Reset" dugme

## 4. DEFINICIJA GOTOVOG (Definition of Done)
- [ ] `/rooms` stranica sa Grid i Floor Plan view modom
- [ ] RoomCard komponenta sa color-coded statusom
- [ ] RoomDetail modal sa svim informacijama
- [ ] Filter bar (status, building, floor, search)
- [ ] Status legenda
- [ ] Floor Plan view sa po spratovima
- [ ] Status change kroz modal (sa permisijama)
- [ ] Responzivni grid (6→4→2→1 kartica)
- [ ] Azuriran `STATUS.md`

## 5. RESTRIKCIJE
- Floor Plan view koristi jednostavan grid po spratovima (ne SVG mapu)
- Boje statusa su konzistentne sa design sistemom
- RoomDetail modal ne radi direktan API call — koristi vec ucitane podatke
- Filteri se primjenjuju na frontendu (ako su svi podaci ucitani) ili kroz API
- Refresh dugme ponovo ucitava podatke sa servera
