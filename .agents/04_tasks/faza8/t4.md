# ZADATAK T8.4: Frontend — Profil gosta
**Status:** [ ] PENDING
**Faza:** 8 - Gost i profil
**Modul:** frontend/src/
**Vlasnik:** AI Coding Agent

## 1. CILJ
Kreirati detaljnu stranicu profila gosta koja prikazuje licne podatke, istoriju boravaka, preferencije i trenutni aktivni boravak (ako postoji).

## 2. KONTEKST (Referenca)
- FSD_07: Gosti i CRM

## 3. TEHNICKI ZAHTJEVI

### 3.1 Komponente
```
src/components/guests/
  GuestProfile.tsx               — glavna stranica profila
  GuestInfoCard.tsx              — kartica sa licnim podacima
  GuestDocumentList.tsx          — lista dokumenata
  GuestStayHistoryTable.tsx      — tabela istorije boravaka
  GuestPreferencesForm.tsx       — forma za preferencije
  GuestStatsCard.tsx             — kartica sa statistikom
  ActiveStayCard.tsx             — kartica trenutnog boravka (ako postoji)
```

### 3.2 Podaci i API integracija
- `GET /api/guests/{id}` — osnovni podaci
- `GET /api/guests/{id}/history` — istorija
- `GET /api/guests/{id}/preferences` — preferencije
- `GET /api/guests/{id}/stats` — statistika
- `GET /api/bookings?guestId={id}&status=CheckedIn` — aktivni boravak

### 3.3 Layout
- Lijeva kolona: GuestInfoCard, GuestDocumentList, GuestPreferencesForm
- Desna kolona: ActiveStayCard (ako postoji), GuestStatsCard, GuestStayHistoryTable
- Tabovi unutar desne kolone: History | Stats | Documents
- Edit mode: klik na "Edit" mijenja info u formu

### 3.4 Specijalni zahtjevi
- Ako je gost trenutno na check-inu (CheckedIn): prikazati ActiveStayCard sa dugmadima za brze akcije
- Ako gost nema preferencija: prikazati praznu formu sa "Add preferences" dugmetom
- Dokumenti: prikazani kao kartice sa statusom (Verified / Not Verified)
- GDPR banner: ako gost nema GDPR consent, prikazati warning banner

## 4. DEFINICIJA GOTOVOG (Definition of Done)
- [ ] GuestProfile stranica prikazuje sve podatke
- [ ] ActiveStayCard se prikazuje samo ako je gost CheckedIn
- [ ] Edit mode funkcionalan za osnovne podatke i preferencije
- [ ] History tabela paginirana
- [ ] Quick actions na ActiveStayCard rade
- [ ] GDPR warning banner implementiran
- [ ] Loading i error state implementirani
- [ ] Azuriran `STATUS.md`

## 5. RESTRIKCIJE
- Zavisi od: T8.1, T8.2, T8.3, T4.3
- Ne implementirati OCR/MRZ na frontendu — to je T8.5
- Dokumenti se prikazuju read-only (upload je T8.5)
- GDPR banner ne blokira koristenje stranice — samo upozorenje
